import 'package:flutter/material.dart';

class YeniAnne extends StatelessWidget {
  const YeniAnne({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}