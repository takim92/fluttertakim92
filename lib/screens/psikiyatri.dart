import 'package:flutter/material.dart';

class Psikiyatri extends StatelessWidget {
  const Psikiyatri({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}