import 'package:flutter/material.dart';

class Diyetisyenlik extends StatelessWidget {
  const Diyetisyenlik({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}